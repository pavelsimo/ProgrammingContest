#include <cstdio>
#include <cstdarg>


#define SIZE 4096

int main(int argc, char *argv[]) {

	int N, x;
	char buf[SIZE];
	while ( gets(buf) != NULL ) {
		N = strlen(buf);
		
	}
	
	return 0;
}
